@model IEnumerable<WebGamingSDV.Models.VirtualM>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New VirtualMachine</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.publicIp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.login)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.password)
            </th>
            <th> @Html.DisplayNameFor(model => model.name)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.publicIp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.login)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.password)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.name">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.name">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.name">Delete</a> |

                <a asp-action="PowerOff" asp-route-id="@item.name">PowerOff</a>

                <a asp-action="Connect" asp-route-id="@item.name">Connect</a>

               @* <form asp-action="PowerOn">
                    <input type="hidden" asp-for="@item.name" />
                    <input type="submit" value="PowerOn" class="btn btn-success" /> |
                </form>
                <form asp-action="PowerOff">
                     <input type="hidden" asp-for="@item.name" />
                    <input type="submit" value="PowerOff" class="btn btn-danger" /> |
                </form>*@
            </td>
        </tr>
}
    </tbody>
</table>
